import Layout from "../components/Layout";

export default function StudentsPage() {
  return (
    <Layout>
      <div>
        <h2 className="text-xl font-semibold mb-4">Danh sách sinh viên (mẫu)</h2>
        <p className="text-sm text-slate-500">Trang đang ở chế độ demo với dữ liệu giả.</p>
      </div>
    </Layout>
  );
}

